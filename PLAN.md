# EDITOR_KIM 계획

이 문서는 현재 상태와 남은 작업만 추적합니다. 완료된 세부 변경은 각 Release 설명과 커밋 이력을 기준으로 합니다.

## 목표
- PDF 텍스트 객체를 원본 레이아웃과 폰트를 최대한 유지하며 수정
- 민감한 텍스트를 단순 덮기가 아니라 PDF에서 제거하고 검증
- 비개발자도 터미널이나 API 키 없이 구독형 AI를 연결
- 원본 파일을 보호하고 편집 결과를 예측 가능하게 저장

## 구조

```text
Electron UI
  ├─ local HTTP/SSE server
  ├─ PDFium WASM engine
  ├─ Claude Code CLI
  └─ Codex App Server (ChatGPT login)
```
문서 파일은 로컬에 남습니다. AI 대화는 현재 문서 텍스트를, 폰트 추천은 선택 영역 이미지·문구·지원 폰트 목록을 선택한 공급자에게 전송합니다.

## 완료
- **PDF 편집**: 렌더링, 텍스트 편집, 폰트 유지, 폴백, 여러 줄, 폭 맞춤, 이동, 그룹, 실행 취소
- **가리기**: 선택 문자 제거 후 재추출 검증, 이미지 위 투명 검색용 텍스트 편집, 검색 결과에서 줄 전체 가리기
- **폰트 맞추기**: TTF 직접 지정, 미리보기, AI 추천(버튼 클릭 시에만)
- **페이지 도구 v0.8.0**: 추출, 병합, 내보내기, 이미지 삽입, 용량 줄이기, 바로 열기
- **페이지 도구 v0.9.0(P5)**: 검색(Ctrl+F, 강조·이전/다음), 쪽 회전(도구줄·축소본 우클릭), 축소본 드래그로 순서 변경, 분할(N쪽씩), 축소본에서 쪽 하나만 새 파일로 저장, 여러 파일 용량 줄이기(문서를 열지 않고 처리), 회전 페이지에서도 정렬되는 오버레이 좌표 변환
- **UX(P5)**: 상태줄(쪽 이동·확대), 사이드바 파일/페이지 탭, 빈 상태·드롭 오버레이, 도구줄 묶음 재편, 용어 통일("가리기")
- **Markdown**: 편집과 안전한 실시간 미리보기
- **AI**: Claude Code·Codex, 기능 사용 시점에만 로그인 요청, WinGet 설치
- **앱**: 확대(커서 기준, 폭 맞춤), 단일 인스턴스, 포트 폴백, 아이콘, 테스트·CI

## 다음 작업

### v0.10.0 안정화 (우선순위순)

직원이 매일 쓰다가 막히거나 잘못 판단하게 되는 것, 데이터가 깨지거나 앱이 죽을 수 있는 것만 먼저 고친다.

1. **긴 작업 진행 표시·취소** — 용량 줄이기(최대 12회 반복)·이미지 내보내기·병합 중 화면이 멈춘 것처럼 보인다. 서버에 작업 ID와 취소 플래그(반복·페이지 사이에서 검사), UI에 진행 창("3/10쪽", "시도 4/12")과 취소 버튼. 취소하면 스냅샷으로 원상 복구.
2. **실행 취소 메모리 상한** — 문서 전체 바이트 스냅샷 20개는 30MB 스캔 문서에서 600MB까지 간다. 문서당 총 바이트 상한(예: 256MB, 최소 3단계 보장)으로 오래된 것부터 버리고 상한에 걸리면 상태줄에 알린다.
3. **로그인 만료 안내** — CLI 인증 오류를 잡아 "로그인이 만료됐습니다 → [다시 로그인]"으로 기존 로그인 흐름에 연결, 상태줄 AI 표시는 "재로그인 필요".
4. **파괴적 작업 뒤 토스트 실행 취소** — 페이지 삭제·용량 줄이기·회전 완료 토스트에 5초간 "실행 취소" 버튼(기존 undo 라우트 재사용).
5. **용량 줄이기 조기 종료** — 이전 시도보다 줄지 않으면 중단, 목표 미달이면 최소 결과로 확정. 100쪽 스캔 문서로 소요 시간 실측.
6. **회전 페이지 이미지 크기 조절 검증** — 회전 0·90·180·270에서 삽입·드래그·손잡이 조절을 실제 이미지로 실측하고 어긋나면 수정.
7. **실제 창 수동 확인** — 병합·이미지 내보내기·이미지 삽입의 파일 대화상자와 연결 프로그램 열기를 Electron 창에서 확인.

결정이 필요한 것: 코드 서명(인증서 구매 vs 사내 IT 예외 등록), 회사 PC의 WinGet 정책·프록시 환경 첫 설치 동행.

### 이후

- 인라인 편집 + 얇은 도구막대, 텍스트·이미지 우클릭 메뉴, 앱 안 단축키 도움말, AI 패널 비용 문구 정리
- 검색 결과의 글자 단위 가리기(검색 결과 → 원본 조각 매핑이 불확실해 보류)
- OTF/CFF·가변 폰트와 여러 줄 폰트 지정, 외부 TTF 등록 영속화
- 스캔 PDF의 OCR 연동 여부 검토
- PNG 이미지 삽입이 Electron 전용인 제약 해소

## 배포 기준
1. `npm test` 통과
2. `npm audit`에서 알려진 취약점 0건
3. 실제 PDF에서 편집·마스킹·저장·재열기 수동 확인
4. Claude와 ChatGPT(Codex) 설치·로그인·응답 확인
5. 사용자 승인 후에만 portable/setup EXE 생성 및 Release 게시

## 알려진 한계
- Windows 중심이며 macOS/Linux 배포는 검증하지 않았습니다.
- 이미지 안 글자 편집과 OCR 데이터 제거는 지원하지 않습니다.
- 모든 PDF 폰트·합자·세로쓰기 조합의 완전한 보존을 보장하지 않습니다.
- 이미지와 투명 검색용 텍스트가 함께 있는 PDF는 그림 글자의 모양과 다를 수 있습니다.
- AI 결과는 사용자가 검토한 뒤 적용해야 합니다.
