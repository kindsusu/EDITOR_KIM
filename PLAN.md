# EDITOR_KIM 계획

이 문서는 공개 저장소에서 현재 상태와 다음 작업만 추적합니다. 완료된 세부 변경은 [CHANGELOG.md](CHANGELOG.md)를 기준으로 합니다.

## 목표

- PDF 텍스트 객체를 원본 레이아웃과 폰트를 최대한 유지하며 수정
- 민감한 텍스트를 단순 덮기가 아니라 PDF에서 제거하고 검증
- 비개발자도 터미널이나 API 키 없이 구독형 AI를 연결
- 원본 파일을 보호하고 편집 결과를 예측 가능하게 저장

## 현재 구조

```text
Electron UI
  ├─ local HTTP/SSE server
  ├─ PDFium WASM engine
  ├─ Claude Code CLI
  └─ Codex App Server (ChatGPT login)
```

문서 파일은 로컬에 남습니다. AI 기능을 사용할 때만 현재 문서 텍스트가 선택한 공급자에게 전송됩니다. 인증은 각 CLI가 관리하며 EDITOR_KIM은 키나 토큰을 읽지 않습니다.

## 완료

- PDF 렌더링, 텍스트 객체 편집, 저장 및 다른 이름으로 저장
- 임베드 폰트 유지와 맑은 고딕 폴백 서브셋
- 부분 마스킹, 사각형 마스킹, 제거 후 텍스트 재추출 검증
- 여러 줄 편집, 폭 맞춤, 그룹, 이동, 실행 취소/다시 실행
- 투명 검색용 텍스트 편집과 이미지 위 잔상 보정
- 빠른 확대/문서 전환 시 오래된 렌더 무효화
- Markdown 편집, 미리보기, 변경안 비교
- Claude Code 및 ChatGPT(Codex) 공급자 선택과 스트리밍
- WinGet 기반 원클릭 설치와 브라우저 로그인
- 독립적인 AI 패널 접기/펼치기
- 자동 엔진·AI/UI 테스트와 GitHub Actions CI
- Ctrl+휠·Ctrl+=/−/0 확대/축소(커서 기준 고정, 폭 맞춤, 배율 기억), 고해상도 렌더, 페이지 지연 로딩
- AI 응답 중지, Enter 전송, 세션 유실 시 자동 재시도, 저장 실패 알림, 서버 기준 미저장 상태 동기화

## 다음 작업

- 여러 제조사의 실제 PDF 회귀 샘플 확대
- 스캔 PDF OCR 연동 여부 검토
- 긴 문서의 undo 메모리 사용량 최적화(현재 문서당 전체 바이트 스냅샷 20개)
- 공급자 오류·로그인 만료 메시지 추가 개선(로그인 만료 시 안내 문구)
- 코드 서명과 설치 파일 배포 절차 확정

## 배포 기준

1. `npm test` 통과
2. `npm audit`에서 알려진 취약점 0건
3. 실제 PDF에서 편집·마스킹·저장·재열기 수동 확인
4. Claude와 ChatGPT(Codex) 설치·로그인·응답 확인
5. 사용자 승인 후에만 portable/setup EXE 생성 및 Release 게시

## 알려진 한계

- Windows 중심이며 macOS/Linux 배포는 검증하지 않았습니다.
- 이미지 안 글자 편집과 OCR 데이터 제거는 지원하지 않습니다.
- 모든 PDF 폰트·합자·세로쓰기 조합의 완전한 보존을 보장하지 않습니다.
- AI 결과는 사용자가 검토한 뒤 적용해야 합니다.
